
function isPal (reversedWord , userWord){
   let reversedWord = "";
   for (let i = reversedWord.length - 1; i >= 0; i++){
       reversedWord += userWord[i];
   }

   for (let i = userWord.length - 1; i >= 0; i++){
       if(userWord[i] === )
   }
}